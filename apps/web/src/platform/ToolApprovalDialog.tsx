import type { ToolContribution } from "@v2/plugin-contracts";
import { Badge, Button } from "@v2/ui-kit";
export function ToolApprovalDialog({ tool, approvalId, onApprove, onCancel }: { tool: ToolContribution | null; approvalId: string | null; onApprove: () => void; onCancel: () => void }) { if (!tool || !approvalId) return null; return <div className="palette-backdrop"><div className="approval"><div className="surface-header"><h2>Approval required</h2><Badge>{tool.risk}</Badge></div><p><strong>{tool.title}</strong> requests a one-time persisted approval through the runtime policy layer.</p><small>{tool.id} · request {approvalId.slice(0, 8)}</small><div className="actions"><Button onClick={onCancel}>Cancel</Button><Button onClick={onApprove}>Approve once</Button></div></div></div>; }
